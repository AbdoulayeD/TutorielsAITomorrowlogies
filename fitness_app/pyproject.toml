[tool.poetry]
name = "fitai"
version = "0.1.0"
description = "AI-powered fitness tracker built with Streamlit and Gemini"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
streamlit = ">=1.32.0"
google-genai = ">=1.0.0"
python-dotenv = ">=1.0.0"
pandas = ">=2.0.0"
plotly = ">=5.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
